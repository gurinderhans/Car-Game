using UnityEngine;
using System.Collections;

public class DriveCamResize : MonoBehaviour {
	/*
	Rect smallScreen=new Rect(0.65f,0.55f,0.30f,0.40f);
	bool driveMode=true;

	public Camera guncam;
	//Camera driveCam;

	void Start(){
		//**set the cameras**
		guncam = GameObject.FindGameObjectWithTag ("GunCam").camera;
		//driveCam = GameObject.FindGameObjectWithTag ("MainCamera").camera;
	}
	void Update () {
		//cameraMovement scriptShootCam = shootCam.gameObject.GetComponent<cameraMovement> ();
		if(Input.GetKeyDown(KeyCode.Y)){
			if(driveMode){
				camera.rect=smallScreen;
				camera.depth+=2;
				//print("Drive Camera's depth is "+ camera.depth);
				print ("shoot mode is true");

				//driveCam.GetComponent<GUILayer>().enabled = false;
				//guncam.GetComponent<GUILayer>().enabled = true;
				this.GetComponent<GUILayer>().enabled = false;

				driveMode=false;
			}
			else{
				camera.rect = new Rect(0,0,Screen.width,Screen.height);
				//GameObject.FindGameObjectWithTag("GunCam").camera.GetComponent<GUILayer>().enabled = true;
				driveMode=true;
			}
		}
	}*/
	
}